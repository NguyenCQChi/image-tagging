import { Box } from '@mui/material';

const Navigation = () => {
  const container = {
    padding: '25px 35px'
  }
  return (
    <Box sx={container}>
      Navigation
    </Box>
  )
}

export default Navigation;